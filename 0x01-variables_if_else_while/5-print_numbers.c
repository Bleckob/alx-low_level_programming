#include <stdio.h>

/**
* main - Entry point
*
* Description: a program that writes alphabets in lowercase
*
* Return: always return 0 (success)
*/

int main(void)
{
	char b;

	for (b = '0'; b <= '9'; b++)
		putchar(b);
putchar('\n');
	return (0);
}
